---
phase: 01-stripping-path-interactive-mode-only
plan: 05
type: execute
wave: 3
depends_on: [01-01-PLAN, 01-02-PLAN, 01-03-PLAN, 01-04-PLAN]
files_modified:
  - README.md
  - .planning/phases/01-stripping-path-interactive-mode-only/01-EXIT-GATE-RESULT.md
autonomous: false
requirements: [INST-01, INST-02, CAP-02, CAP-03]
must_haves:
  truths:
    - "`README.md` at the repo root has a SAGOL intro paragraph in both Korean and English, a `Quick attach to your project` section showing the two-line install (`bunx sagol init && bunx sagol doctor`), and a short `Architecture note — server-side stripping` section pointing to `.planning/research/HEADLESS_HOOK_LIMITATION.md`."
    - "`01-EXIT-GATE-RESULT.md` captures the result of running EVERY Phase 1 exit gate (verify-server-strip, live HARD GATE receipt, leakage check, doctor) in sequence, with pass/fail status for each, timestamps, and a final verdict line."
    - "If ANY exit gate fails at run-time, this plan is marked `autonomous: false` so the human sees the failure before it gets rubber-stamped."
    - "All four D-15 exit gate items are cross-referenced and resolved in `01-EXIT-GATE-RESULT.md`."
  artifacts:
    - path: "README.md"
      provides: "Project README — intro, install, architecture note"
      contains: "bunx sagol init"
      min_lines: 30
    - path: ".planning/phases/01-stripping-path-interactive-mode-only/01-EXIT-GATE-RESULT.md"
      provides: "Phase 1 exit gate consolidation — one file, all 4 gates, pass/fail per gate, verdict"
      contains: "D-15"
  key_links:
    - from: "README.md"
      to: ".planning/research/HEADLESS_HOOK_LIMITATION.md"
      via: "explicit link in architecture note"
      pattern: "HEADLESS_HOOK_LIMITATION"
    - from: ".planning/phases/01-stripping-path-interactive-mode-only/01-EXIT-GATE-RESULT.md"
      to: "01-LIVE-HARDGATE.md, 01-LEAKAGE-CHECK.md"
      via: "cross-reference"
      pattern: "01-LIVE-HARDGATE|01-LEAKAGE-CHECK"
---

<objective>
Ship the install documentation for D-15 gate item #4's "README paragraph" requirement AND consolidate every Phase 1 exit gate into a single `01-EXIT-GATE-RESULT.md` file so the orchestrator can read one file to confirm Phase 1 is actually closed.

Purpose: The other four plans each deliver pieces of the exit gate, but there is no one-file "did Phase 1 actually pass?" artifact. This plan is the wall-of-truth that proves D-15 is satisfied. It also delivers the bilingual install README paragraph (D-13 requirement).

Output:
- `README.md` — new or updated, contains the install + architecture blurb
- `.planning/phases/01-stripping-path-interactive-mode-only/01-EXIT-GATE-RESULT.md` — the phase-close receipt
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-stripping-path-interactive-mode-only/01-CONTEXT.md
@.planning/research/HEADLESS_HOOK_LIMITATION.md
@.planning/phases/01-stripping-path-interactive-mode-only/01-LIVE-HARDGATE.md
@.planning/phases/01-stripping-path-interactive-mode-only/01-LEAKAGE-CHECK.md
@scripts/doctor.ts
@scripts/verify-server-strip.ts
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Write README.md with bilingual intro + install + architecture note</name>
  <files>README.md</files>
  <read_first>
    - .planning/PROJECT.md (for the canonical Core Value + Korean project description — the README mirrors this tone)
    - .planning/research/HEADLESS_HOOK_LIMITATION.md (to link + summarize the architecture reason in one sentence)
    - .planning/phases/01-stripping-path-interactive-mode-only/01-CONTEXT.md (D-10 one-paragraph rationale for the architecture note)
    - package.json (to cite the current pinned deps for the install section)
    - If a README.md already exists at repo root, read it first — we AUGMENT, not replace (preserve any non-conflicting content)
  </read_first>
  <action>
    1. If `README.md` does not exist at the repo root, create it. If it exists, read it and place the new content at the top, preserving any existing content below a horizontal rule.

    2. The top of README.md MUST have these sections in order:

       a. `# SAGOL (사골)` — h1 title.

       b. A Korean paragraph (2–4 sentences) introducing SAGOL, taken from `.planning/PROJECT.md` "What This Is" but condensed:
          > SAGOL은 Claude Code Skill/MCP로 동작하는 **에이전트 보고서 라우터 + 컨텍스트 청결 도구**다. 서브에이전트 출력을 디스크의 정형 보고서로 분리하고, 메인 컨텍스트에는 짧은 stripped form만 남겨서 컨텍스트 오염을 줄인다. 이름은 "Claude Code를 사골처럼 쪽쪽 우려먹는다"는 밈에서 나왔다.

       c. An English paragraph (2–4 sentences) — parallel content:
          > SAGOL is a **sub-agent report router + context hygiene tool** for Claude Code, shipped as an MCP server. It routes sub-agent outputs into durable markdown reports on disk and replaces the tool response in the main agent's context with a ≤200-char stripped form, so report bodies never accumulate in the parent conversation. The name is a Korean food-meme pun meaning "simmer Claude Code like bone broth."

       d. A `## Status` line: `v1 Spike — Phase 1 (stripping path) complete. Phase 2 (dashboard + bidirectional feedback) in progress. Kill-switch verdict deferred to a manual benchmark session post-Phase 2.`

       e. A `## Quick attach to your project` section with exactly this content:
          ```markdown
          ## Quick attach to your project

          ```sh
          # From the root of your Claude Code project
          bunx sagol init    # merges .mcp.json + .claude/settings.json (preserves existing entries)
          bunx sagol doctor  # confirms SAGOL is wired
          ```

          Requires Bun 1.3.11+ and Claude Code 2.1.108+. See `.planning/research/PINNED_VERSIONS.md`.

          After `bunx sagol init`, restart your Claude Code session once so the new MCP server subprocess is spawned. Sub-agents calling `mcp__sagol__write_report` will then see the stripped tool response; full report bodies persist to `.sagol/reports/<id>.md` on disk.
          ```
          (Preserve the triple-backtick code fence above. Note the nested code block.)

       f. A `## Architecture note — server-side stripping` section with exactly this content:
          ```markdown
          ## Architecture note — server-side stripping

          SAGOL Phase 0 discovered that project-local `PostToolUse` hooks do not fire in `claude -p` headless mode on Claude Code 2.1.108 (see `.planning/research/HEADLESS_HOOK_LIMITATION.md`). The Phase 1 HARD GATE re-verification then confirmed the same limitation applies to interactive mode. Rather than modify global settings (explicitly forbidden by our D-08 decision), SAGOL moved the stripping logic **inside the MCP server itself** — the `handleWriteReport` handler in `src/mcp/server.ts` returns only the stripped form, so the MCP stdio subprocess (which IS spawned identically in both modes) becomes the hypothesis-bearing surface. The dormant hook path (`scripts/strip-report.ts`) is preserved as reference code in case a future Claude Code version fixes project-local hook loading.
          ```

       g. A `## Links` section with bullets to:
          - `.planning/PROJECT.md` — Core Value + constraints
          - `.planning/ROADMAP.md` — phase plan
          - `.planning/research/HEADLESS_HOOK_LIMITATION.md` — the architecture finding
          - `.planning/research/PINNED_VERSIONS.md` — pinned CC + Bun + dep versions

       h. A horizontal rule `---` separating the new content from any preserved pre-existing README content below.

    3. Run `grep -c "bunx sagol init" README.md` — must be ≥ 1.
    4. Run `grep -c "HEADLESS_HOOK_LIMITATION" README.md` — must be ≥ 1.
    5. Run `grep -c "사골" README.md` — must be ≥ 1 (Korean present).
    6. Run `grep -c "SAGOL is a" README.md` — must be ≥ 1 (English present).
  </action>
  <verify>
    <automated>test -f README.md && grep -q "bunx sagol init" README.md && grep -q "HEADLESS_HOOK_LIMITATION" README.md && grep -q "사골" README.md && grep -q "server-side stripping" README.md && [ $(wc -l < README.md) -ge 30 ]</automated>
  </verify>
  <acceptance_criteria>
    - `README.md` exists at the repo root.
    - `wc -l README.md` returns ≥ 30.
    - `grep -q "# SAGOL" README.md` succeeds.
    - `grep -q "사골" README.md` succeeds (Korean paragraph present).
    - `grep -q "SAGOL is a" README.md` succeeds (English paragraph present).
    - `grep -q "## Quick attach to your project" README.md` succeeds.
    - `grep -q "bunx sagol init" README.md` succeeds.
    - `grep -q "bunx sagol doctor" README.md` succeeds.
    - `grep -q "## Architecture note" README.md` succeeds.
    - `grep -q "HEADLESS_HOOK_LIMITATION" README.md` succeeds.
    - `grep -q "server-side stripping" README.md` succeeds.
    - `grep -q "D-08" README.md` succeeds (cross-reference to the decision ID).
    - `grep -q "PINNED_VERSIONS" README.md` succeeds.
  </acceptance_criteria>
  <done>README.md satisfies D-13's "README paragraph" requirement: bilingual intro, two-line install, architecture rationale with link to the headless-hook finding.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Run all 4 Phase 1 exit gates in sequence and record 01-EXIT-GATE-RESULT.md</name>
  <read_first>
    - .planning/phases/01-stripping-path-interactive-mode-only/01-CONTEXT.md (D-15 — the 4 exit gates verbatim)
    - .planning/phases/01-stripping-path-interactive-mode-only/01-LIVE-HARDGATE.md (must exist from 01-01)
    - .planning/phases/01-stripping-path-interactive-mode-only/01-LEAKAGE-CHECK.md (must exist from 01-02)
    - scripts/verify-server-strip.ts (gate 1)
    - scripts/doctor.ts (gate 4)
  </read_first>
  <files>.planning/phases/01-stripping-path-interactive-mode-only/01-EXIT-GATE-RESULT.md</files>
  <action>
    Executor runs each gate in sequence, captures exit code + output for each, and writes `01-EXIT-GATE-RESULT.md` summarizing pass/fail per gate. This is the single file the orchestrator reads to know Phase 1 is closed.

    1. Executor: run `bun run scripts/verify-server-strip.ts`, capture exit code (gate 1 — expect 0).
    2. Executor: confirm `.planning/phases/01-stripping-path-interactive-mode-only/01-LIVE-HARDGATE.md` exists and contains a `Canary present in tool response: no` line (gate 2 — the receipt from 01-01 task 1).
    3. Executor: confirm `.planning/phases/01-stripping-path-interactive-mode-only/01-LEAKAGE-CHECK.md` exists and ends with `PASS` (gate 3 — the receipt from 01-02 task 2).
    4. Executor: run `bun run scripts/doctor.ts`, capture exit code (gate 4 first half — expect 0).
    5. Executor: confirm `README.md` exists with the `Quick attach to your project` section (gate 4 second half — from this plan's task 1).
    6. Executor: write `.planning/phases/01-stripping-path-interactive-mode-only/01-EXIT-GATE-RESULT.md` with:
       - A markdown table with 4 rows (one per D-15 gate item), columns: `# | Gate | Source artifact | Status | Evidence`
       - Captured exit codes for `verify-server-strip.ts` and `doctor.ts`
       - A traceability block mapping each of INST-01, INST-02, CAP-01, CAP-02, CAP-03, CAP-04, CAP-05 to the gate that validates it
       - A final verdict line: `PHASE 1 COMPLETE` (if all 4 gates GREEN) or `PHASE 1 BLOCKED — <which gate>` (if any RED)
    7. If ANY gate is RED → STOP, surface the failure to the user (this task is `autonomous: false` so the human sees it); do NOT write `PHASE 1 COMPLETE`.
    8. Resume-signal: Executor shows the exit-gate table + the final verdict. User types `approved` (if verdict is PHASE 1 COMPLETE) or reports the issue (if blocked).
  </action>
  <resume-signal>Executor shows the exit-gate table + the final verdict. User types `approved` (if verdict is PHASE 1 COMPLETE) or reports the issue (if blocked).</resume-signal>
  <verify>
    <automated>test -f .planning/phases/01-stripping-path-interactive-mode-only/01-EXIT-GATE-RESULT.md && grep -q 'PHASE 1 COMPLETE' .planning/phases/01-stripping-path-interactive-mode-only/01-EXIT-GATE-RESULT.md && bun run scripts/verify-server-strip.ts && bun run scripts/doctor.ts && grep -q '^PASS' .planning/phases/01-stripping-path-interactive-mode-only/01-LEAKAGE-CHECK.md && grep -q 'Canary present in tool response: no' .planning/phases/01-stripping-path-interactive-mode-only/01-LIVE-HARDGATE.md</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/phases/01-stripping-path-interactive-mode-only/01-EXIT-GATE-RESULT.md` exists.
    - The file contains a table with 4 rows, one per D-15 gate item.
    - The file references `D-15` as the source of truth.
    - The file references each source artifact: `01-LIVE-HARDGATE.md`, `01-LEAKAGE-CHECK.md`, `scripts/verify-server-strip.ts`, `scripts/doctor.ts`, `README.md`.
    - The file contains captured exit codes for the live runs of `verify-server-strip.ts` and `doctor.ts`.
    - The file's last non-blank line is either `PHASE 1 COMPLETE` or `PHASE 1 BLOCKED — <reason>`.
    - `bun run scripts/verify-server-strip.ts` currently exits 0.
    - `bun run scripts/doctor.ts` currently exits 0.
    - `.planning/phases/01-stripping-path-interactive-mode-only/01-LIVE-HARDGATE.md` currently contains `Canary present in tool response: no`.
    - `.planning/phases/01-stripping-path-interactive-mode-only/01-LEAKAGE-CHECK.md` currently ends with `PASS`.
    - The file cross-references each of INST-01, INST-02, CAP-01, CAP-02, CAP-03, CAP-04, CAP-05 in a traceability line and states which gate validates each.
  </acceptance_criteria>
  <done>Single-file exit gate report committed. All 4 D-15 gates are RESOLVED in writing, all 7 phase requirement IDs are traced, and the verdict is either `PHASE 1 COMPLETE` or a clearly-stated block that the human must resolve before transition.</done>
</task>

</tasks>

<verification>
- `README.md` exists with all required sections.
- `.planning/phases/01-stripping-path-interactive-mode-only/01-EXIT-GATE-RESULT.md` exists with 4-row gate table and `PHASE 1 COMPLETE` verdict (or human-blocking failure).
- `bun run scripts/verify-server-strip.ts` exits 0.
- `bun run scripts/doctor.ts` exits 0.
- `01-LIVE-HARDGATE.md` confirms no canary in tool response.
- `01-LEAKAGE-CHECK.md` ends with `PASS`.
- `bun test tests/mcp-server.test.ts` still exits 0 (regression floor).
</verification>

<success_criteria>
- D-15 gate item #4 (doctor GREEN + README paragraph) fully closed.
- Every Phase 1 requirement ID (INST-01, INST-02, CAP-01…CAP-05) is traced to a gate and an artifact in `01-EXIT-GATE-RESULT.md`.
- The orchestrator can read ONE file (`01-EXIT-GATE-RESULT.md`) to confirm Phase 1 is complete and can advance to Phase 2.
- No touches to `~/.claude/settings.json`. No new dependencies. No caveman lift.
</success_criteria>

<output>
After completion, create `.planning/phases/01-stripping-path-interactive-mode-only/01-05-SUMMARY.md`
</output>
