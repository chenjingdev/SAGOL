---
phase: 01-stripping-path-interactive-mode-only
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/01-stripping-path-interactive-mode-only/01-LIVE-HARDGATE.md
  - src/mcp/server.ts
  - tests/mcp-server.test.ts
  - package.json
autonomous: false
requirements: [CAP-01, CAP-02, CAP-04]
must_haves:
  truths:
    - "The live Claude Code interactive session, after a fresh session restart on CC 2.1.108, returns only the stripped form from `mcp__sagol__write_report` — the canary token used in the live call does NOT appear in the tool response the main agent sees."
    - "`bun run scripts/verify-server-strip.ts` exits 0 (direct-import proof remains GREEN after any edits)."
    - "`bun test tests/mcp-server.test.ts` exits 0 and covers `buildStripped`, `deriveSummary`, and `handleWriteReport` frontmatter/round-trip behavior."
    - "`deriveSummary` is a named export on `src/mcp/server.ts` so tests and future callers can import it."
  artifacts:
    - path: ".planning/phases/01-stripping-path-interactive-mode-only/01-LIVE-HARDGATE.md"
      provides: "Live CC round-trip HARD GATE receipt — timestamp, CC version, Bun version, observed stripped tool response, report id, canary-absence confirmation."
      contains: "CC version: 2.1.108"
    - path: "tests/mcp-server.test.ts"
      provides: "bun test unit suite covering buildStripped + deriveSummary + handleWriteReport"
      contains: "import { describe, it, expect } from \"bun:test\""
    - path: "src/mcp/server.ts"
      provides: "Server-side stripping handler; exports deriveSummary alongside buildStripped + handleWriteReport"
      contains: "export function deriveSummary"
  key_links:
    - from: "tests/mcp-server.test.ts"
      to: "src/mcp/server.ts"
      via: "named imports"
      pattern: "from \"\\.\\./src/mcp/server"
    - from: ".planning/phases/01-stripping-path-interactive-mode-only/01-LIVE-HARDGATE.md"
      to: "live Claude Code interactive session run"
      via: "human pastes real tool response"
      pattern: "\\[report:\\d+-[0-9a-f]+\\]"
---

<objective>
Prove the server-side stripping mechanism (committed in de66c83) is **live** in a freshly restarted Claude Code interactive session on the pinned CC 2.1.108 / Bun 1.3.11 stack, then lock the three functions the hypothesis depends on (`buildStripped`, `deriveSummary`, `handleWriteReport`) under a `bun test` unit suite so future edits can't silently regress.

Purpose: D-15 exit gate item #1 (verify-server-strip GREEN) and #2 (Live CC round-trip HARD GATE) are the foundation for every other Phase 1 plan. Until we have a canary-free tool response observed inside a real CC session (not just the direct-import harness), the entire architecture after the 2026-04-15 pivot is unvalidated. Unit tests added alongside are the cheapest insurance against regression — they cost ~80 LOC and cover the three code paths that carry the hypothesis.

Output:
- `.planning/phases/01-stripping-path-interactive-mode-only/01-LIVE-HARDGATE.md` (new — the receipt)
- `tests/mcp-server.test.ts` (new — bun test unit suite)
- `src/mcp/server.ts` edited to `export function deriveSummary`
- `package.json` gains a `test` script alias (`bun test`)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-stripping-path-interactive-mode-only/01-CONTEXT.md
@.planning/phases/01-stripping-path-interactive-mode-only/01-RESEARCH.md
@.planning/research/HEADLESS_HOOK_LIMITATION.md
@src/mcp/server.ts
@scripts/verify-server-strip.ts
@.mcp.json
@.claude/settings.json

<interfaces>
<!-- Extracted from src/mcp/server.ts (commit de66c83). Executor should import directly. -->

From src/mcp/server.ts:
```ts
export function buildStripped(args: {
  id: string;
  title: string;
  summary: string;
}): string;

// NOT YET EXPORTED — task 2 adds the `export` keyword
function deriveSummary(body: string): string;

export async function handleWriteReport(input: {
  title: string;
  body: string;
  source?: string;
}): Promise<{ content: Array<{ type: "text"; text: string }> }>;
```

The handler writes the full body to `${SAGOL_PROJECT_ROOT ?? process.cwd()}/.sagol/reports/<id>.md`, then returns ONLY the stripped form in `content[0].text`. The report id shape is `<epochMs>-<8hex>` and is matched by `/^\[report:(\d+-[0-9a-f]+)\] /`.
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Live HARD GATE re-verification in fresh Claude Code session (human-driven)</name>
  <read_first>
    - src/mcp/server.ts (confirm handleWriteReport returns `buildStripped` output and the full body is not in the tool response)
    - .planning/phases/01-stripping-path-interactive-mode-only/01-CONTEXT.md (D-10 rationale, D-15 exit gate item 2)
    - .planning/research/HEADLESS_HOOK_LIMITATION.md (Server-side workaround section — this is what we are re-verifying)
  </read_first>
  <files>.planning/phases/01-stripping-path-interactive-mode-only/01-LIVE-HARDGATE.md</files>
  <action>
    Executor has already confirmed `bun run scripts/verify-server-strip.ts` is GREEN (direct-import harness). This task is the live round-trip: the executor generates a canary token, instructs the user to restart Claude Code, the user pastes a single prompt that calls `mcp__sagol__write_report` with the canary in the body, and the executor (resumed in the same or a new session) records the observed tool response in `01-LIVE-HARDGATE.md`.


    1. Executor: run `bun run scripts/verify-server-strip.ts` one more time and confirm exit 0 (sanity check that the code still compiles and the handler still strips).
    2. Executor: generate a fresh canary token AND write it to a scratch file (so the verify step can re-read it at runtime without placeholder substitution):
       ```bash
       CANARY_HEX=$(node -e "console.log(require('crypto').randomBytes(16).toString('hex'))")
       umask 177 && printf '%s' "$CANARY_HEX" > /tmp/sagol-hardgate-canary.txt
       ```
       Then print `CANARY_HEX` to the user so they can visually confirm the expected token.
    3. Executor: print the EXACT prompt the user must paste into the fresh Claude Code session (`<CANARY_HEX>` replaced by the real token):
       ```
       Call the mcp__sagol__write_report tool with these exact arguments:
         title: "Phase 1 live HARD GATE re-verification"
         body:  "canary: <CANARY_HEX>\n\nThis is the Phase 1 live HARD GATE check. The server MUST return ONLY the stripped form. The full body belongs on disk only."
         source: "phase-1-live-hardgate"
       Then paste the FULL tool response back to me verbatim in a code block.
       ```
    4. Executor: instruct the user to (a) fully quit Claude Code (`/exit` or close the terminal), (b) run `claude --version` to confirm `2.1.108`, (c) start a new interactive session from this project directory, (d) paste the prompt above, (e) paste the tool response back to the executor.
    5. Human: performs restart + paste, returns the tool response. Executor captures it into the shell variable `TOOL_RESPONSE` (e.g., via heredoc).
    6. Executor: **BEFORE writing `01-LIVE-HARDGATE.md`**, run the in-memory honesty grep:
       ```bash
       if printf '%s' "$TOOL_RESPONSE" | grep -qF "$CANARY_HEX"; then
         echo "LEAK DETECTED — canary appeared in live tool response; STOP and escalate"
         exit 1
       fi
       ```
       Only if this exits 0 may the executor proceed to write the receipt.
    7. Executor: write `01-LIVE-HARDGATE.md` with all required lines listed in `<acceptance_criteria>`, then run the on-disk grep:
       ```bash
       DISK_PATH=.sagol/reports/$(printf '%s' "$TOOL_RESPONSE" | sed -nE 's/^\[report:([0-9]+-[0-9a-f]+)\].*/\1/p').md
       grep -c "$CANARY_HEX" "$DISK_PATH"  # expect ≥ 1
       ```
       Record `yes`/`no` on the appropriate lines in `01-LIVE-HARDGATE.md`.
    8. Executor: after the file is written and verified, delete the scratch canary:
       ```bash
       rm -f /tmp/sagol-hardgate-canary.txt
       ```
       (This step runs AFTER `<verify><automated>` below — the verify step reads `/tmp/sagol-hardgate-canary.txt` so it must still exist at verify time. Clean up in the plan summary commit step.)
  </action>
  <how-to-verify>
    1. The tool response the user pastes back matches the pattern `^\[report:\d+-[0-9a-f]+\] Phase 1 live HARD GATE re-verification\n`.
    2. The tool response does NOT contain the canary hex the executor generated.
    3. The tool response contains the substring `(full body persisted to .sagol/reports/` followed by the same id.
    4. `.sagol/reports/<id>.md` exists on disk and DOES contain the canary hex (executor runs `grep -c "$CANARY_HEX" .sagol/reports/<id>.md` and expects ≥ 1).
    5. `claude --version` output is exactly `2.1.108` (or the user confirms the version verbally).
  </how-to-verify>
  <resume-signal>User pastes the tool response OR reports a failure. If canary appears in the tool response → STOP, Phase 1 is dead and orchestrator must escalate to the user for architectural reassessment (the D-10 workaround itself has broken).</resume-signal>
  <verify>
    <automated>test -s /tmp/sagol-hardgate-canary.txt && grep -q '^\[report:[0-9]\+-[0-9a-f]\+\] Phase 1 live HARD GATE re-verification' .planning/phases/01-stripping-path-interactive-mode-only/01-LIVE-HARDGATE.md && ! grep -qFf /tmp/sagol-hardgate-canary.txt .planning/phases/01-stripping-path-interactive-mode-only/01-LIVE-HARDGATE.md && grep -q "Canary present in tool response: no" .planning/phases/01-stripping-path-interactive-mode-only/01-LIVE-HARDGATE.md && [ "$(grep '^CC version:' .planning/phases/01-stripping-path-interactive-mode-only/01-LIVE-HARDGATE.md | awk '{print $3}')" = "$(claude --version 2>/dev/null | awk '{print $1}')" ]</automated>
  </verify>
  <acceptance_criteria>
    - File `.planning/phases/01-stripping-path-interactive-mode-only/01-LIVE-HARDGATE.md` exists.
    - File contains a line starting with `CC version:` followed by `2.1.108` (and this matches live `claude --version` output).
    - File contains a line starting with `Bun version:` followed by `1.3.11`.
    - File contains a line starting with `Timestamp:` with an ISO-8601 value.
    - File contains a fenced code block with the observed tool response, which matches `^\[report:\d+-[0-9a-f]+\] Phase 1 live HARD GATE re-verification`.
    - File documents the canary hex value on a separate line prefixed `Canary (for post-hoc audit):` and a separate line prefixed `Canary present in tool response:` with value `no`.
    - File contains a line `Disk file path:` pointing to a real file under `.sagol/reports/`.
    - File contains a line `Disk file contains canary:` with value `yes` (executor confirms via grep before writing).
    - The scratch file `/tmp/sagol-hardgate-canary.txt` does NOT appear as a substring anywhere in `01-LIVE-HARDGATE.md` when matched via `grep -Ff /tmp/sagol-hardgate-canary.txt` (i.e. the actual canary hex is absent from the receipt file — integrity gate).
    - Executor ran action step 6 (in-memory pre-write grep) and step 7 (post-write disk grep) in that order. Action step 6 exited 0 before `01-LIVE-HARDGATE.md` was written.
  </acceptance_criteria>
  <done>`01-LIVE-HARDGATE.md` committed with all required lines. Executor confirmed via in-memory grep (step 6) BEFORE writing the receipt that the canary is absent from the tool-response block, then via disk grep (step 7) that the full body IS on disk. `verify-server-strip.ts` also GREEN at this point. `/tmp/sagol-hardgate-canary.txt` is deleted in the final commit step of this plan's summary phase (action step 8).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Export deriveSummary and add bun test unit suite</name>
  <files>src/mcp/server.ts, tests/mcp-server.test.ts, package.json</files>
  <read_first>
    - src/mcp/server.ts (to see current `deriveSummary` / `buildStripped` / `handleWriteReport` / `buildMarkdown` signatures)
    - scripts/verify-server-strip.ts (to see how the direct-import harness instantiates the handler — mirror the SAGOL_PROJECT_ROOT convention in tests)
    - .planning/phases/01-stripping-path-interactive-mode-only/01-RESEARCH.md section 4 ("bun test Adoption") — the exact minimum-viable test list
  </read_first>
  <behavior>
    - Test 1 (`buildStripped`): returns a string starting with `[report:<id>] <title>\n` and containing the summary and the `.sagol/reports/<id>.md` path, and is ≤500 chars for a typical summary.
    - Test 2 (`buildStripped`): does NOT include the raw body — callers pass only `{ id, title, summary }`, so the function has no way to leak body; test asserts no substring of a 2KB random body can appear.
    - Test 3 (`deriveSummary`): returns at most 200 characters.
    - Test 4 (`deriveSummary`): collapses internal whitespace runs (newlines, tabs, multiple spaces) to single spaces in the returned string.
    - Test 5 (`deriveSummary`): uses only the first non-empty paragraph when the body has multiple paragraphs separated by blank lines.
    - Test 6 (`deriveSummary`): handles a body that is whitespace-only without throwing (returns empty string or trimmed fallback).
    - Test 7 (`deriveSummary`): leaves a short single-paragraph body under 200 chars unchanged after whitespace collapse.
    - Test 8 (`handleWriteReport` round-trip): given a canary body, the returned tool-response text does NOT contain the canary AND the on-disk file at `${SAGOL_PROJECT_ROOT}/.sagol/reports/<id>.md` DOES contain the canary.
    - Test 9 (`handleWriteReport` round-trip): the returned text matches `^\[report:\d+-[0-9a-f]+\]`.
    - Test 10 (`handleWriteReport` round-trip): a unicode title like `사골 테스트` round-trips into the frontmatter without corrupting YAML — disk file contains `title: "사골 테스트"` literally (the yamlEscape helper wraps in quotes, so the expected substring is `title: "사골 테스트"`).
    - Test 11 (`handleWriteReport`): two consecutive calls in the same test return distinct ids.
  </behavior>
  <action>
    1. Edit `src/mcp/server.ts` at line 48: change `function deriveSummary` to `export function deriveSummary`. No other change to the file.
    2. Create `tests/mcp-server.test.ts`:
       - Import: `import { describe, it, expect, beforeAll, afterAll } from "bun:test";`
       - Import: `import { buildStripped, deriveSummary, handleWriteReport } from "../src/mcp/server.ts";`
       - Import: `import { mkdtempSync, rmSync, existsSync, readFileSync } from "node:fs";`
       - Import: `import { tmpdir } from "node:os";`
       - Import: `import { join } from "node:path";`
       - In `beforeAll`: create a unique temp dir via `mkdtempSync(join(tmpdir(), "sagol-test-"))`, set `process.env.SAGOL_PROJECT_ROOT` to that path. Store the path in a top-level `let tmpRoot`.
       - In `afterAll`: `rmSync(tmpRoot, { recursive: true, force: true })`.
       - Write the 11 tests listed in `<behavior>` above. For Test 8 use `const canary = "BUNTEST_CANARY_" + crypto.randomUUID().replace(/-/g, "")` and assert `!result.content[0].text.includes(canary)` AND `readFileSync(join(tmpRoot, ".sagol/reports", id + ".md"), "utf8").includes(canary) === true` where `id` is parsed from the tool response via `text.match(/^\[report:(\d+-[0-9a-f]+)\]/)[1]`.
       - For Test 10: pass `title: "사골 테스트"` and `grep` the disk file for the literal substring `title: "사골 테스트"`.
    3. Edit `package.json`: in `scripts`, add `"test": "bun test"` (preserving other scripts; do not remove `canary`, `noise-gate`, `mcp`, `doctor`).
    4. Run `bun test tests/mcp-server.test.ts` and iterate until all 11 tests pass. The `buildStripped` + `deriveSummary` behavior is already implemented — tests should pass without touching the implementation beyond the `export` keyword added in step 1.
    5. Run `bun run scripts/verify-server-strip.ts` one final time and confirm exit 0 (the test suite should not have regressed the direct-import harness).
  </action>
  <verify>
    <automated>bun test tests/mcp-server.test.ts 2>&1 | tail -5 && bun run scripts/verify-server-strip.ts</automated>
  </verify>
  <acceptance_criteria>
    - `src/mcp/server.ts` contains the literal string `export function deriveSummary`.
    - `tests/mcp-server.test.ts` exists.
    - `tests/mcp-server.test.ts` contains the literal strings `import { describe, it, expect` and `from "bun:test"` and `handleWriteReport` and `deriveSummary` and `buildStripped` and `SAGOL_PROJECT_ROOT` and `mkdtempSync`.
    - `package.json` contains `"test": "bun test"` inside the `scripts` object.
    - `bun test tests/mcp-server.test.ts` exits 0.
    - `bun test tests/mcp-server.test.ts` output contains the literal substring `11 pass` (bun test summary format) OR a stronger equivalent showing all 11 tests passed.
    - `bun run scripts/verify-server-strip.ts` exits 0 after the test suite has run (no state bleed from temp dirs).
    - `grep -c "SAGOL_PROJECT_ROOT" tests/mcp-server.test.ts` is ≥ 1.
  </acceptance_criteria>
  <done>Unit suite committed and GREEN. `deriveSummary` is exported. `package.json` test script wired. Direct-import harness still GREEN. No behavior change in `src/mcp/server.ts` beyond the `export` keyword.</done>
</task>

</tasks>

<verification>
- `bun run scripts/verify-server-strip.ts` exits 0.
- `bun test tests/mcp-server.test.ts` exits 0 (11/11 pass).
- `.planning/phases/01-stripping-path-interactive-mode-only/01-LIVE-HARDGATE.md` exists with all required lines and NO canary inside the pasted tool response block.
- `grep -q 'export function deriveSummary' src/mcp/server.ts` succeeds.
</verification>

<success_criteria>
- D-15 gate item 1 (verify-server-strip GREEN) confirmed one more time post-edits.
- D-15 gate item 2 (Live CC round-trip) recorded in `01-LIVE-HARDGATE.md` with zero canary leakage.
- Three hypothesis-bearing functions (`buildStripped`, `deriveSummary`, `handleWriteReport`) locked under unit tests.
- No new npm dependencies (bun test is built-in).
- No touch of `~/.claude/settings.json` (D-08 preserved).
- `scripts/strip-report.ts` unchanged and still dormant (D-10 revival optionality preserved).
</success_criteria>

<output>
After completion, create `.planning/phases/01-stripping-path-interactive-mode-only/01-01-SUMMARY.md`
</output>
